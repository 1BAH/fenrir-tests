#! /usr/bin/env sh

docker rm -f remote

for i in `docker network inspect -f '{{range .Containers}}{{.Name}} {{end}}' sem0-net`; do
    docker network disconnect -f network_name "$i"
done

docker network rm -f sem0-net
