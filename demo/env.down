#! /usr/bin/env sh


docker rm -f qwerty
cont="$(loki -c 'eval' 'echo $FENRIR_TMP_DIR')/env"

for i in `docker network inspect -f '{{range .Containers}}{{.Name}} {{end}}' network_name`; do
    docker network disconnect -f network_name $i
done


docker network rm -f demo-net
